---
// An address no school answers to: the app, out of the index, with a toast.
import Base from '../layouts/Base.astro';
import { HOME_HEAD } from '../prerender';
---
<Base {...HOME_HEAD} noindex notFound />
