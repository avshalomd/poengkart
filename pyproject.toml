[project]
name = "poengkart-tools"
version = "0"
description = "The Poengkart data pipeline: county extractors, the forecast model, the dataset builds"
requires-python = ">=3.9"
# Dependencies live in tools/requirements.txt, the one list CI and the cloud
# setup script install; nothing installs this project, so it repeats none.

[tool.pytest.ini_options]
testpaths = ["tools/tests"]
